# 0x04-TypeScript

Ready-to-upload project containing tasks 0–5 with TypeScript, Webpack (where needed), and configs.
Each task directory is self-contained. Build each task individually:
```bash
cd task_X
npm i
npm run build
```
